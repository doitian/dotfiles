default:
  merge_top_pillar:
    user: user
    group: group
    private: private

  private: False
  user: {{ grains.username }}
  group: {{ grains.groupname }}
  repos_dir: '{{ salt['file.join']('.dotfiles', 'repos') }}'

  repos: []
  phrases: []